'use client'
interface Props {
    kinoId:string,
    ugugdul:object
}
export default function SingleMovie (props:Props) {
    const { kinoId, ugugdul } = props

    return <div>
        test
    </div>
}
